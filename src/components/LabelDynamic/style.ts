import styled from "styled-components";

export const LabelDynamicContainer = styled.p`
    color: ${(props) => props.theme['yellow-500']};
    font-size: 3.125rem;

    span{
        text-transform: uppercase;
    }
`
