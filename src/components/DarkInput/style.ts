import styled from "styled-components";

export const DarkInputContainer = styled.input`
    background: ${(props) => props.theme['black-300']};
    border: none;
    font-size: 0.9375rem;
    padding: 1rem 3rem;
`
